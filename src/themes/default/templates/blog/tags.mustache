{{<html}}
{{$main}}

{{& page.contents.html}}

<div id="tags" class="wrapper">

    {{#empty(tags)}}
    Empty.
    {{/empty(tags)}}
    {{^empty(tags)}}
    <div class="grid grid-321">
    {{#tags}}
        {{> partials.blog.tag}}
    {{/tags}}
    
    </div>
    {{/empty(tags)}}
</div>

{{/main}}
{{/html}}

type: author
paths:
    - blog/authors

properties:
    title:
        type: string
        required: true

relations:

queries:
    posts:
        contentType: post
        scope: list
        filter:
            key: authors
            operator: contains
            value: "{{id}}"
        orderBy:
            - key: publication
              direction: desc
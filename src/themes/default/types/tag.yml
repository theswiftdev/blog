id: tag
template: blog.tag.default

context:
    site:
        tags:
            sort: title
            order: asc
            pagination:
                limit: 10
                
    local:
        posts:
            references: post
            foreignKey: tags
            sort: publication
            order: desc


